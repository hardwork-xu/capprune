# 安全与运行边界

[English](SECURITY.md) · [架构](docs/zh/ARCHITECTURE.md)

CapPrune 0.1.0 是本地 CPU 库与 CLI，不是托管服务或安全边界。它不提供鉴权、租户隔离、加密或生产服务等级承诺。本地验证配置为 macOS arm64 CPU 上的 Python 3.12，其他平台需要各自验证。

使用可信数值输入及可信索引归档。拒绝异常数组、避免 pickle 反序列化，并不意味着压缩 NPZ 解析器是沙箱：过大或恶意输入仍可能耗尽内存、CPU 时间或磁盘。处理外部文件时应设置操作系统资源限制。聚类分配预算只限制一项临时分配，不限制总内存；两份原始/重排 float64 数据约占 16Nd 字节，尚未计入其他数组和运行时分配。

索引保留数值向量和原始行号，不进行匿名化或加密。不要公开由私人输入构建的索引。路径与诊断输出也可能暴露本地信息，分享前应检查日志。保存输出需要正常文件系统权限，应使用专用目标，避免覆盖无关已有文件。

剪枝证明假设归一化实数向量。数值保护和一致性测试支持文档声明的 float64 行为，但不构成覆盖所有浮点平台的形式化证明。不可信的已保存上界或布局改动可能破坏正确性，因此持久化检查属于支持的加载路径。精确性边界见 [RESEARCH.md](docs/zh/RESEARCH.md)，实测范围见 [EXPERIMENTS.md](docs/zh/EXPERIMENTS.md)。

维护范围限于当前 0.1.x 代码线，不保证响应时间。本地版本没有已配置的公开仓库或经核验的私密安全联系人。请在本地保留最小复现并移除私人数据，待维护者公布认可的私密联系方式后再使用。不要假设存在未列出的地址，也不要公开包含私人信息的漏洞细节。

公开发布前，检查跟踪文件、生成产物、依赖声明，以及提交作者/提交者身份。源码归档排除 `.git`；私人本地 Git 元数据须独立保存，除非已明确审查允许公开。干净的源码归档不能替代对历史 Git 推送内容的检查。
