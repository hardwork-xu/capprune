# 贡献指南

[English](CONTRIBUTING.md) · [维护约定](AGENTS.md) · [开发记录](docs/zh/DEVELOPMENT.md)

我偏好可审阅的小改动，对应已观察到的正确性问题、实测瓶颈或明确的使用需求。核心保持为 CPU 精确检索库；新增后端或服务需要单独定义范围并提供证据。

在项目根目录使用 Python 3.12 和 uv 0.8.22：

```sh
uv sync --frozen
make demo
make check
make build
```

依赖安装后，默认测试与演示可离线运行。`make check` 执行格式、静态、类型、测试及文档检查。涉及测量的改动还需运行：

```sh
uv run python benchmark.py --output results/new.json
uv run python scripts/analyze.py results/new.json
```

每次运行使用新的结果文件名，保留有效但不利的样本及失败记录。保留原始冻结协议；调整协议时使用新文件，记录理由，并将结果标记为另一项实验。不得用实测值替换目标，也不得将 CPU 结果外推到其他后端。

完整贡献应包含可复现触发条件或动机、真正执行计算的相关测试、同步更新的中英文 API/CLI/文档，以及验证命令的真实状态。修改上界、归一化、同分排序、布局或持久化时，需要对应正确性测试。性能比较的两条路径必须使用相同精度、线程数、工作负载和计时范围。标识符和机器可读字段保持英文。

提交采用 Conventional Commits 英文摘要，正文用中文说明改动与验证。保留已有贡献者归属及第三方声明。对项目自有代码的贡献采用 [MIT 许可证](LICENSE)，不得包含许可证不兼容或不明确的材料。

公开产物不包含数据集、凭据、私人日志、个人绝对路径和本地 Git 身份元数据。公开历史前，同时检查文件内容及提交作者/提交者元数据。公开源码归档刻意排除 `.git`，私人本地历史另行保存。不要在普通修复中附带重置历史、修改远端、公开发布或部署操作。

安全问题参见 [SECURITY_zh.md](SECURITY_zh.md)。本地版本尚未配置公开 issue tracker 或私密报告地址；不要在文档中虚构地址，也不要向公开渠道发送敏感报告。
